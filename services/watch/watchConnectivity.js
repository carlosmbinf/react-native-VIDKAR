export const buildWatchUserSnapshot = () => null;

export const clearWatchUserSnapshot = async () => ({ supported: false });

export const readWatchConnectivityStatus = async () => ({ supported: false });

export const sendWatchMessage = async () => ({ supported: false });

export const subscribeToWatchMessages = () => () => {};

export const syncCurrentUserWithWatch = async () => ({ supported: false });

export const syncWatchDashboard = async () => ({ supported: false });

export const transferWatchUserInfo = async () => ({ supported: false });
